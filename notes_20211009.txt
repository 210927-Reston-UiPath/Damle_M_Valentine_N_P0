- Changed `FrontEndInterface` seq to `ClientMenu`. 
	* altered some of the menu options.
	* changed `repeat` from variable to `out` argument.
- Created a `ClientLogin` menu seq with an out argument `clientId`.
- Created a variable on `ClientMenu` seq called `clientId`.

- Took `PlaceOrder` seq.
- Removed logic for finding the shopping list.
- Created `ClientSelectVendorMenu` seq.
- Moved vendor selection logic from `PlaceOrder` seq to `ClientSelectVendorMenu` seq.
